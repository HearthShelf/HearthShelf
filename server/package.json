{
  "name": "hearthshelf-qg-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "HearthShelf QuestGiver AI proxy — holds the model key server-side, enforces per-user rate limits.",
  "main": "index.js",
  "scripts": {
    "prestart": "node scripts/ensure-core-dist.mjs",
    "start": "node index.js",
    "test": "node --test tests/*.test.mjs",
    "recover-admins": "node scripts/recover-admins.js"
  },
  "dependencies": {
    "@github/copilot-sdk": "1.0.11",
    "@hearthshelf/core": "file:../packages/core",
    "@libsql/client": "^0.17.4",
    "adm-zip": "^0.6.0",
    "jose": "^5.9.6",
    "mailparser": "^3.9.16",
    "smtp-server": "^3.13.6"
  }
}
